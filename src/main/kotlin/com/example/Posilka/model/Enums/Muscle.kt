package com.example.Posilka.model.Enums

enum class Muscle {
    // Hrudník
    PECTORALIS_MAJOR,  // Veľký prsný sval
    PECTORALIS_MINOR,  // Malý prsný sval

    // Chrbát
    LATISSIMUS_DORSI,  // Široký sval chrbta
    TRAPEZIUS,         // Trapézový sval
    RHOMBOIDS,         // Rombické svaly
    ERECTOR_SPINAE,    // Vzpřimovače chrbtice

    // Nohy
    QUADRICEPS,        // Štvorhlavý stehenný sval
    HAMSTRINGS,        // Zadné stehenné svaly
    GLUTEUS_MAXIMUS,   // Veľký sedací sval
    GLUTEUS_MEDIUS,    // Stredný sedací sval
    CALVES,            // Lýtkové svaly (Gastrocnemius + Soleus)

    // Core (Stred tela)
    RECTUS_ABDOMINIS,  // Priamy brušný sval (tzv. "six-pack")
    OBLIQUES,          // Šikmé brušné svaly
    TRANSVERSE_ABDOMINIS, // Priečny brušný sval

    // Ramená
    DELTOID,           // Deltový sval
    ROTATOR_CUFF,      // Rotátorová manžeta (súbor svalov)

    // Ruky
    BICEPS,            // Dvojhlavý sval ramena
    TRICEPS,           // Trojhlavý sval ramena
    FOREARM_FLEXORS,   // Ohýbače predlaktia
    FOREARM_EXTENSORS  // Naťahova
}